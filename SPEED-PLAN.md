# CPU 推論の高速化プラン

## 前提

実践環境では GPU が使えない。精度は確保できたので、以降は CPU レイテンシだけを詰める。

計測環境は Ryzen 5 5600X / 6C12T、`--threads 4`、onnxruntime 1.28.0 CPU EP。
評価データは `dataset/ab-eval-general`（日本語、1行あたり **1,144 ノード / 10,608 エッジ /
38 輪郭**、エッジの **70.3% が同一輪郭内**）。モデルは `checkpoints/ab-mix/best.pt`。

**出発点: 80.4 ms/行。**

## 到達点（実測）

| 段階 | 1行あたり | 累積 | 再学習 |
| --- | --- | --- | --- |
| 現状 | 80.4 ms | 1.00x | — |
| + `ScatterND` 化 | 40.8 ms | **1.97x** | **不要** |
| + 捨てるエッジを分類器に通さない | 30.4 ms | **2.64x** | **不要** |
| + `hidden` 128 → 64 | 14.4 ms | 5.59x | 要 |
| + `layers` 4 → 2 | 7.8 ms | 10.29x | 要 |

上2段は**重みを1ビットも変えない**。グルーピング結果が現行モデルと完全一致することを
日本語200行 × 閾値 0.4/0.5/0.6/0.7/0.8 で確認済み（1000/1000 一致）。

---

## Tier A: 再学習なしで 2.64倍 ← まずここ

### A-1. `ScatterElements` を `ScatterND` に置き換える（1.97倍）

README の計測どおり `ScatterElements` が CPU 時間の 43.4% を占めていた。**原因は
op の選択ミス**で、アルゴリズムのせいではない。

```python
# 現状 (model.py:52, 57, 131, 133)
out.scatter_add_(0, dst.unsqueeze(-1).expand(-1, hidden), msg)
```

`.expand(-1, hidden)` が `[E, 128]` の i64 インデックスを実体化する。E=10,608 なら
**10.9 MB のインデックステンソル**を毎回作って、ORT はそれを1要素ずつ舐める。
本当にやりたいのは「行ごとの加算」なので、インデックスは `[E, 1]` で足りる。

素の op 単体で測った差:

| op | インデックス形状 | 時間 |
| --- | --- | --- |
| `ScatterElements` | `[E, 128]` i64 | 7.76 ms |
| **`ScatterND`** | `[E, 1]` i64 | **0.35 ms** |
| （参考）密行列積 `[N,E]×[E,H]` | — | 12.27 ms |

**22倍。** モデルには `ScatterElements` が16個あるので、これがそのまま効く。

#### やり方

`index_put_` を使うと `ScatterND` に落ちる。ただし**罠がある**。

```python
out.index_put_((dst,), msg, accumulate=True)   # -> ScatterND
```

| torch の書き方 | 出る op |
| --- | --- |
| `scatter_add_(0, expanded_idx, v)` | `ScatterElements` |
| `index_add_(0, idx, v)` | `ScatterElements` |
| **`index_put_((idx,), v, accumulate=True)`** | **`ScatterND`** |

**`accumulate=True` が ONNX に伝わらない。** 出力される `ScatterND` に `reduction`
属性が付かず、既定の `"none"`（＝上書き）になる。重複インデックスが前提の
メッセージパッシングでこれをやると**最後に書いた1本しか残らない**。実測では
出力が完全に壊れた（`max|diff| = 0.9999`）のに、エクスポートは成功して
`onnx.checker` も通る。

対処はエクスポート後に属性を足すだけ:

```python
for node in model.graph.node:
    if node.op_type == "ScatterND" and not any(a.name == "reduction" for a in node.attribute):
        node.attribute.append(helper.make_attribute("reduction", "add"))
```

これで `max|diff|` が 1.18e-3 まで落ちる。**ゼロにはならない**（`ScatterND` の加算は
並列で走るので合計の順序が変わる）が、グルーピング判定は 120/120 で一致した。
`export_onnx.py` の既存パリティ検査は許容 1e-4 なので、**閾値をペア確率か
グルーピング一致に変える必要がある**。数値の完全一致は諦めて、判定の一致を見る。

学習側は `scatter_add_` のままでよい。`segment_add(base, index, values)` のような
ヘルパーを1枚挟んで、エクスポート時だけ `index_put_` に切り替えるのが安全。

**工数見込み: 0.5日**（うち半分は検査の作り直し）

### A-2. 捨てられるエッジを分類器に通さない（さらに 1.34倍）

[postprocess.rs:67-69](crates/glyph-infer/src/postprocess.rs#L67-L69) は同一輪郭内の
エッジを `continue` で読み飛ばす。判定単位が輪郭ペアだから当然だが、つまり
**エッジの 71.8% は分類器を通した結果がそのまま捨てられている**。

分類器は1エッジあたり `527 → 128 → 64 → 1` で 75,584 MAC。GAT 層のエッジ側は
1層 512 MAC × 4層 = 2,048 MAC しかない。**エッジに比例する計算の 97% が分類器**で、
その 7割が無駄になっている。

`ScatterND` 適用後のプロファイル（最大グラフ 24,442 エッジ、4スレッド）:

| op | 占有率 | ms/回 | 呼出 |
| --- | --- | --- | --- |
| FusedGemm | 17.7% | 18.21 | 4 |
| Add | 16.8% | 17.31 | 63 |
| Gemm | 14.8% | 15.23 | 42 |
| Gather | 9.1% | 9.33 | 32 |
| Concat | 8.5% | 8.70 | 33 |
| Mul | 7.2% | 7.43 | 14 |
| LeakyRelu | 5.1% | 5.30 | 4 |
| LayerNormalization | 5.0% | 5.20 | 13 |
| **ScatterND** | **3.7%** | 3.81 | 13 |

`ScatterND` は 43.4% → 3.7% に落ちて、上位は全部 `[E, hidden]` の演算になった。
分類器の入力 `[E, 527]` を `[E×0.28, 527]` にすれば、この上位が丸ごと 0.28倍になる。

#### やり方

ONNX の入力を1本増やす（`scored`: 分類するエッジの i64 インデックス）。

```python
def forward(self, x, edge_index, edge_attr, cid, cattr, scored):
    hx, summary = self.model.trunk(x, edge_index, edge_attr, cid, cattr, None)
    return torch.sigmoid(self.model.edge_logits(
        hx, summary, edge_index[:, scored], edge_attr[scored], cid, cattr, None))
```

Rust 側は `graph.rs` が `same_contour` を既に持っているので、
`edge_features[i*4+3] == 0.0` のインデックスを集めて渡すだけ。
`postprocess.rs` は `probs[i]` の添字を `scored[i]` に読み替える。

メッセージパッシング（`trunk`）は全エッジで回すので**受容野も精度も変わらない**。

- 出力テンソルも 0.28倍になるので、Rust への戻しコピーも減る
- `glyph-infer eval` の**点エッジ単位の P/R/F1 は測れなくなる**。輪郭ペア単位の
  指標は残るので実害はないが、README の数字と直接は比べられなくなる
- GUI のエッジ重ね描きも同一輪郭内が消える。診断用に全エッジ版を別エクスポートするか、
  `scored` に全インデックスを渡せば従来どおり動く（入力が可変なので分岐は要らない）

**工数見込み: 0.5日**

---

## Tier B: 再学習ありで 10倍

ここから先は精度とのトレードオフになる。**A を先にやってから測ること。**
A は精度が動かないので、B の A/B が汚れない。

### B-1. モデルを細くする（`hidden` 128 → 64 で 2.1倍）

上位の op が全部 `[E, hidden]` なので、`hidden` は**ほぼ線形に**効く。
`layers` も同様（4 → 2 でさらに 1.8倍）。

そのまま学習し直すより、**現行モデルを教師にした蒸留**を薦める。ペア確率を
ソフトターゲットにすれば、`--pair-loss-weight` の枠組みがそのまま使える。
0.9789 の完全グルーピング一致率をどこまで保てるかを測るのが本題。

### B-2. エッジ数を減らす

CPU は完全にエッジ数に線形（実測 8.8µs/エッジ、切片ほぼ0）なので直接効く。
日本語50行での実測:

| グラフ構築ルール | 無向ペア数/行 | 比 |
| --- | --- | --- |
| 現状（knn 8 + ブリッジ） | 5,789 | 1.00x |
| `--knn 4` | 2,869 | **0.50x** |
| `--spacing 0.10`（点を半分に） | 2,843 | **0.49x** |
| 同一輪郭内をリング（±1,±2）に、輪郭間は knn 8 | 4,145 | 0.72x |

- **`--spacing` のほうが筋がいい**。エッジだけでなくノードも半分になるので、
  ノード側の Gemm（`lin_src`/`lin_dst`/`out`、1層あたり 3×N×128×128）も半分になる
- リング化は期待ほど効かない。0.05em 間隔だと ±1,±2 だけで 2N ペア＝現状の 4割を占める
- **学習と推論でグラフ設定を揃える制約**があるので、データセット再生成が要る

### B-3. 二段構え（カスケード）

フルコーパスでのペア F1 は **0.9993**。つまり**輪郭ペアの判断はほとんど自明**で、
難しいのはごく一部。

1. 輪郭特徴量（8次元）+ ペア幾何（11次元）だけの小さいモデルで全ペアを判定
2. 確率が 0.1〜0.9 に落ちたペアだけ、点レベルの本モデルに回す

1段目は輪郭 38 個・ペア 703 個なのでサブミリ秒。2段目に回る割合が1割なら、
平均レイテンシは1段目 + 本モデルの1割になる。**最悪ケースは変わらない**ので、
レイテンシの上限が要るなら向かない。

---

## Tier C: ランタイムを自作する

ORT をやめて Rust で順伝播を直接書く。`glyph-core` がグラフを作っているので、
**CSR を最初から持てる**のが効く。

- **scatter を gather に反転する**。エッジを宛先ノードでソートして CSR にすれば、
  集約は「各ノードが自分の入力エッジを連続読みして足す」になる。ランダム書き込みも
  アトミックも消えて、`rayon` でノード分割すれば並列効率がそのまま出る
- ORT の CPU 並列効率は現状 **12%**（1→12スレッドで 1.39倍）。これは scatter が
  並列化されないせい。CSR にすれば理屈のうえでは 4〜6倍まで伸びる余地がある
- Gemm は `gemm` クレート、要素演算は融合して1パスにする。上のプロファイルの
  `Add`/`Mul`/`LeakyRelu`/`Concat` で 37.6% あるが、これは全部メモリ往復の代金なので、
  融合すればほぼ消える
- モデルが 844 op しかなく構造も固定なので、書ける規模ではある

**工数見込み: 3〜5日。** Tier A/B をやり切ってから、まだ足りない場合の手。

---

## 効かないと分かっているもの

| 手 | 結果 | 理由 |
| --- | --- | --- |
| int8 量子化 | 1.04倍 | Gemm 系 17.9% しか触らない。支配項が別 |
| DirectML | CPU より遅い | 動的形状のオペが CPU に落ちてパーティションが細切れ |
| `--threads` を増やす | 12スレッドで 1.39倍 | scatter が並列化されない（A-1 後も改善せず） |
| `onnx-simplifier` | GPU には効くが CPU には効かない | 削れる `Shape`/`Reshape` は CPU では元々ほぼ無料 |

`--threads` は A-1 適用後も 4 が最適だった（1: 76.5ms / 4: 39.0ms / 12: 65.5ms）。
12 は 6コアに対する過剰割り当てで逆効果。**既定の 4 のままでよい。**

---

## 前処理も見ておく

推論が 30ms を切ると、前処理（`build_graph`）の比率が上がる。
[graph.rs:259-281](crates/glyph-core/src/graph.rs#L259-L281) のブリッジ探索は
**輪郭ペア × 両輪郭の全点ペア**の総当たりで、輪郭85個・2,615ノードの行では
約 340万回の距離計算になる。KD木は kNN にしか使っていない。

輪郭ごとの bbox で足切りするか、ブリッジ探索も KD木に載せれば消える。
Tier A を入れたあとで実測してから判断する。

---

## 推奨する実施順序

1. **A-1（`ScatterND`）** — 1.97倍、再学習なし、半日。`reduction` 属性の罠に注意
2. **A-2（分類するエッジを絞る）** — さらに 1.34倍、再学習なし、半日
3. ここで **30 ms/行**。前処理の実測を取り直して、必要なら bbox 足切り
4. **B-1（蒸留で細くする）** — 精度の許容範囲を測りながら。ここが本番
5. 足りなければ **B-2（グラフを粗く）**、それでも足りなければ **C（自作ランタイム）**

## 検証方法

A の2段は精度が変わらないはずなので、**グルーピング一致で検査する**。

```bash
uv run python -m glyph_gnn.eval --checkpoint ... --data dataset/ab-eval-general
cargo run --release -p glyph-infer -- --model model.onnx --cpu \
    eval --shard dataset/ab-eval-general/shard_00000.msgpack
```

`export_onnx.py` のパリティ検査は 1e-4 では通らなくなるので、
**ペア確率の一致**か**グルーピングの一致**に置き換えること。数値のずれ
（1e-3 台）は `ScatterND` の加算順序が非決定的なせいで、バグではない。
